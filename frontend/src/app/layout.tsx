import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import { Providers } from "@/components/providers";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "ParcelHub — Universal Shipment Tracking",
  description:
    "Track all your packages in one place. Supports Delhivery, BlueDart, DHL, FedEx, UPS, India Post and more.",
  keywords: ["shipment tracking", "package tracking", "courier tracking", "parcel"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="dark">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
